<%= f.input :pagina,
  label: 'Página en Fuente:',
  required: false,
  maxlength: 150
%>

<%= f.input :textob,
  label: 'Texto',
  required: false,
  input_html: {
    "data-toggle" => 'tooltip',
    "title" => 'Se buscarán raices de las palabras que ingrese. Se buscarán separadas.'
  }
%>

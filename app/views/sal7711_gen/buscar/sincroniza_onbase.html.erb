<h1>Sincronización</h1>
<% if @cprob != '' %>
  <p>Se encontraron los siguientes problemas en <%= @numregprob %> registros:</p>
  <hr/>
  <%= raw(@cprob) %>
  <hr/>
<% end %>
<p>La base local tenía <%= @numexiste %> registros. Se examinaron <%= @examinados %> registros pendientes de los cuales se copiaron <%= @procesados %> de OnBase a Sal7711.</p>
<table>
  <% @sinc.each do |f| %>
    <tr><td><%= f %></td></tr>
  <% end %>
</table>

